# Connector skeleton (copy template)

When adding a new connector under `connectors/<name>/`: copy the contents of this directory, replace `<name>`, write the tool modules.

Will be populated with the first concrete connector (`weather/`) as a reference implementation — until then it's documentation only.

Spec with concrete code skeleton: [`../../docs/connector-skeleton.md`](../../docs/connector-skeleton.md).
