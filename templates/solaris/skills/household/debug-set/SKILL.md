---
name: debug-set
description: Retired.
retired: true
version: 2.0.0
---

Retired: `/debug on` said debug mode was on, but nothing in Solaris ever stored or
read that setting, so the answer was never true. This file stands in for a deletion
because ServiceBay only ever overwrites an asset it has delivered, it never removes
one.
