---
name: audit-query
description: Retired.
retired: true
version: 2.0.0
---

Retired: `/audit` reported on cloud-LLM calls, and Solaris has been local-model-only
for a long time — it never makes any, so the command answered every question out of
an empty log. This file stands in for a deletion because ServiceBay only ever
overwrites an asset it has delivered, it never removes one.
